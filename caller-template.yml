# Part of chiibitsu/gates by Angeline S. Viray (Chiibitsu Labs). MIT. https://github.com/chiibitsu/gates
# Copy this to .github/workflows/gates.yml in your repo. The SHA below is v1.0.0, and it
# appears twice on purpose: a reusable workflow cannot discover its own commit, so the
# ref you pin has to be handed to it. Change both together.
on: [pull_request, push]
jobs:
  gates:
    uses: chiibitsu/gates/.github/workflows/gates.yml@17861ae1a5b2679a879a04aa125972c257c1cbda
    with:
      gates_ref: 17861ae1a5b2679a879a04aa125972c257c1cbda
