# Part of chiibitsu/gates by Angeline S. Viray (Chiibitsu Labs). MIT. https://github.com/chiibitsu/gates
# Copy this to .github/workflows/gates.yml in your repo. The SHA below is v1.0.2, and it
# appears twice on purpose: a reusable workflow cannot discover its own commit, so the
# ref you pin has to be handed to it. Change both together.
on: [pull_request, push]
jobs:
  gates:
    uses: chiibitsu/gates/.github/workflows/gates.yml@c3e3f49f91c3c39fc4a74d0ccf6bb8b15d70e3fd # v1.0.2
    with:
      gates_ref: c3e3f49f91c3c39fc4a74d0ccf6bb8b15d70e3fd # v1.0.2
