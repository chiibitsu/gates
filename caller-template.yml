# Part of chiibitsu/gates by Angeline S. Viray (Chiibitsu Labs). MIT. https://github.com/chiibitsu/gates
# Copy this to .github/workflows/gates.yml in your repo. The SHA below is v1.0.1, and it
# appears twice on purpose: a reusable workflow cannot discover its own commit, so the
# ref you pin has to be handed to it. Change both together.
on: [pull_request, push]
jobs:
  gates:
    uses: chiibitsu/gates/.github/workflows/gates.yml@242866820ab5b4aa527efdeefe20bf5ac36e11d2
    with:
      gates_ref: 242866820ab5b4aa527efdeefe20bf5ac36e11d2 # v1.0.1
