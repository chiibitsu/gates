# One path per line. Files must exist and be tracked; directories must exist.
# Add a line when something's absence has bitten once. Remove nothing without a reason
# in the commit. This is the toolkit's own copy; every consumer repo keeps its own at
# the same path, and that is where required-files.sh reads it from.
README.md
LICENSE
CITATION.cff
selftest.sh
caller-template.yml
.ci-allowed-refs
.github/workflows/gates.yml
.github/workflows/selftest.yml
.github/workflows/caller-smoke.yml
# The gate files themselves are NOT listed here. gates/MANIFEST.txt is the one place their
# names are written down, and selftest.sh asserts it against the gates/ directory in both
# directions and checks each one is git-tracked. Listing them here as well made the same
# fact live in two files that nothing compared, so adding a gate took two edits and
# forgetting the second was silent.
gates/MANIFEST.txt
scripts/gates/denylist.txt
fixtures
